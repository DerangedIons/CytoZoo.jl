import{_ as t,o,c as a,ao as i}from"./chunks/framework.ClvvTM6M.js";const g=JSON.parse('{"title":"Design Notes","description":"","frontmatter":{},"headers":[],"relativePath":"reference/design.md","filePath":"reference/design.md","lastUpdated":null}'),r={name:"reference/design.md"};function s(n,e,d,l,c,h){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="Design-Notes" tabindex="-1">Design Notes <a class="header-anchor" href="#Design-Notes" aria-label="Permalink to &quot;Design Notes {#Design-Notes}&quot;">​</a></h1><p>Why the architecture looks the way it does. The archival records, with full context and supersession history, are the Architecture Decision Records in <code>docs/adr/</code> in the repository; this page carries the summary that users and contributors actually need.</p><h2 id="Models-Are-Hand-Coded-Structs,-Not-Symbolic" tabindex="-1">Models Are Hand-Coded Structs, Not Symbolic <a class="header-anchor" href="#Models-Are-Hand-Coded-Structs,-Not-Symbolic" aria-label="Permalink to &quot;Models Are Hand-Coded Structs, Not Symbolic {#Models-Are-Hand-Coded-Structs,-Not-Symbolic}&quot;">​</a></h2><p>CytoZoo previously carried a ModelingToolkit.jl-backed path, with <code>symbolic_system</code> / <code>has_symbolic_system</code> interface hooks and an MTK-based model. It was removed.</p><p><strong>Consequence:</strong> models are hand-coded callable structs, and the base package keeps <strong>zero runtime dependencies</strong>. Everything requiring an external package — solving, tissue coupling, automatic differentiation — arrives through a package extension.</p><p>The trade is deliberate. A symbolic layer buys automatic simplification and code generation, but costs a heavyweight dependency, compile latency, and control over the exact arithmetic in the hot loop. For a package whose value is a <em>registry of validated models with predictable performance</em>, hand-written right-hand sides matching a reference implementation to <code>1e-10</code> are worth more than symbolic manipulation.</p><h2 id="Coupling-Is-Monolithic,-Not-Operator-Splitting" tabindex="-1">Coupling Is Monolithic, Not Operator Splitting <a class="header-anchor" href="#Coupling-Is-Monolithic,-Not-Operator-Splitting" aria-label="Permalink to &quot;Coupling Is Monolithic, Not Operator Splitting {#Coupling-Is-Monolithic,-Not-Operator-Splitting}&quot;">​</a></h2><p>This is the load-bearing decision, and it was made by measurement rather than taste.</p><h3 id="What-was-tried" tabindex="-1">What was tried <a class="header-anchor" href="#What-was-tried" aria-label="Permalink to &quot;What was tried {#What-was-tried}&quot;">​</a></h3><p>Coupling was <strong>originally built on operator splitting</strong>, via <code>OrdinaryDiffEqOperatorSplitting.jl</code>. The reasoning at the time was sound: Lie–Trotter and Strang splitting have well-established convergence theory, each operator can use its own inner solver, and shared state lives in one global vector so shared variables genuinely are the same memory rather than two copies kept in sync by callbacks.</p><p>Two splitting backends existed. The second was a hand-rolled adaptive Lie–Trotter loop, written because the library&#39;s adaptive sub-solves were broken for stiff subsystems — itself a signal about the maintenance burden.</p><h3 id="What-the-measurements-showed" tabindex="-1">What the measurements showed <a class="header-anchor" href="#What-the-measurements-showed" aria-label="Permalink to &quot;What the measurements showed {#What-the-measurements-showed}&quot;">​</a></h3><p>A non-invasive prototype compared splitting against assembling one combined right-hand side:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Criterion</th><th style="text-align:right;">Operator splitting</th><th style="text-align:right;">Monolithic single-RHS</th></tr></thead><tbody><tr><td style="text-align:right;">Error vs. analytic, <code>dt = 0.1</code></td><td style="text-align:right;">4.25e-2</td><td style="text-align:right;">1.88e-10</td></tr><tr><td style="text-align:right;">Error vs. analytic, <code>dt = 0.05</code></td><td style="text-align:right;">2.14e-2</td><td style="text-align:right;">4.73e-12</td></tr><tr><td style="text-align:right;">Error vs. analytic, <code>dt = 0.025</code></td><td style="text-align:right;">1.08e-2</td><td style="text-align:right;">1.31e-13</td></tr><tr><td style="text-align:right;">Stiff <code>share</code> pair (K = 500)</td><td style="text-align:right;">NaN / −Inf with an explicit inner solver</td><td style="text-align:right;">stable under <code>Rodas5P</code></td></tr><tr><td style="text-align:right;">Allocations per RHS call</td><td style="text-align:right;">0 B</td><td style="text-align:right;">0 B</td></tr></tbody></table><p>The splitting error halves as <code>dt</code> halves — textbook first-order Lie–Trotter behaviour. The monolithic right-hand side has <em>no splitting error at all</em>, so its accuracy is bounded only by the integrator&#39;s own truncation order: roughly ten orders of magnitude better on the same solver at the same step size, at identical allocation cost.</p><h3 id="The-decision" tabindex="-1">The decision <a class="header-anchor" href="#The-decision" aria-label="Permalink to &quot;The decision {#The-decision}&quot;">​</a></h3><p><a href="/CytoZoo.jl/dev/reference/api#CytoZoo.CoupledModel"><code>CoupledModel</code></a> is a real functor <code>(cm)(dU, U, p, t)</code> assembled from its submodels and integrated by a single ODE solver. The splitting backends were <strong>deleted</strong> rather than kept as an opt-in path — no consumer justified maintaining a second, strictly less accurate solve path.</p><h3 id="Consequences" tabindex="-1">Consequences <a class="header-anchor" href="#Consequences" aria-label="Permalink to &quot;Consequences {#Consequences}&quot;">​</a></h3><ul><li><p>One solver spans the whole system: no splitting error, and a stiff coupling can use one implicit method across every component.</p></li><li><p><code>OrdinaryDiffEqOperatorSplitting</code> is not a dependency. The solve path needs only a SciMLBase stack, through <code>ext/SciMLBaseExt.jl</code>.</p></li><li><p>Because every component writes into one shared <code>dU</code>, &quot;which equation governs a shared slot&quot; is resolved by <strong>ordering plus zeroing</strong> — owner last, non-owner contributions zeroed — rather than by a splitting schedule. That is where <a href="/CytoZoo.jl/dev/reference/api#CytoZoo.share"><code>share</code></a>&#39;s hard-discard semantics come from. A contributory share (<code>op = +</code>) works in the same shared <code>dU</code>, saving and restoring the slot around each member&#39;s write so contributions sum without any ordering at all.</p></li><li><p>Higher-order (Strang) splitting, once listed as future work, is moot.</p></li></ul><h2 id="Priorities-That-Shaped-the-Coupling-API" tabindex="-1">Priorities That Shaped the Coupling API <a class="header-anchor" href="#Priorities-That-Shaped-the-Coupling-API" aria-label="Permalink to &quot;Priorities That Shaped the Coupling API {#Priorities-That-Shaped-the-Coupling-API}&quot;">​</a></h2><p>In order: 2. <strong>Performance</strong> — within roughly 1% of uncoupled component performance, with no allocations in the hot path.</p><ol start="3"><li><p><strong>A simple <em>authoring</em> API</strong> — someone writing a single component writes today&#39;s <code>f(du, u, p, t)</code> and learns nothing new in order to participate in a <code>share</code>.</p></li><li><p><strong>A simple <em>coupling</em> API.</strong></p></li></ol><p>Priority 2 explains why <code>share</code> requires no authoring change at all, and why <code>connect</code>&#39;s receiver contract (<a href="/CytoZoo.jl/dev/reference/api#CytoZoo.parameter_index"><code>parameter_index</code></a> plus <a href="/CytoZoo.jl/dev/reference/api#CytoZoo.writable_parameters"><code>writable_parameters</code></a>) is the <em>only</em> thing coupling asks a model author to add.</p><h2 id="Switching-Is-Composition,-Not-a-Primitive" tabindex="-1">Switching Is Composition, Not a Primitive <a class="header-anchor" href="#Switching-Is-Composition,-Not-a-Primitive" aria-label="Permalink to &quot;Switching Is Composition, Not a Primitive {#Switching-Is-Composition,-Not-a-Primitive}&quot;">​</a></h2><p>A <code>switch=</code> / <code>switches=</code> API was prototyped and <strong>removed as redundant</strong>. Composing with or without a subsystem or an edge already expresses every on/off case, and it makes the &quot;OFF invariant&quot; — that disabling a module recovers the validated baseline — automatic rather than something to be tested.</p><p>Composition therefore resolves at <code>couple()</code> time and never through <code>p</code>, which carries time- and space-varying payload rather than structural configuration. See <a href="./../guides/coupling/patterns">Patterns Cookbook</a>.</p><h2 id="Open-Questions" tabindex="-1">Open Questions <a class="header-anchor" href="#Open-Questions" aria-label="Permalink to &quot;Open Questions {#Open-Questions}&quot;">​</a></h2><p>Carried forward deliberately, not overlooked:</p><ul><li><p><strong>Flux injection as a distinct edge kind.</strong> Additive contribution itself is shipped, as <code>share(…; op = +)</code> — Modelica&#39;s <em>through</em> (flow) variable alongside the <em>across</em> variable that hard-discard <code>share</code> implements. What was not built is the alternative spelling <code>inject(:X =&gt; :J, :D =&gt; :w)</code>, sourcing a named DERIVED flux: a monitor source is resolved in a pre-pass, so it may not also receive a <code>connect</code> edge, and its value passes through <code>_connect_value</code>, which would strip the term out of every derivative. Revisit only for a module that cannot carry its contribution as a state.</p></li><li><p><strong>GPU compatibility.</strong> Not precluded; the design keeps structs isbits-friendly. Not shipped or tested.</p></li><li><p><strong>Non-ODE components.</strong> DAE and algebraic-constraint coupling remain out of scope.</p></li><li><p><strong>Nested coupling.</strong> Supported, since <code>CoupledModel &lt;: AbstractCardiacCellModel</code>, but the layout-collapsing logic deserves review against a real use case.</p></li></ul><p>See <a href="./../guides/coupling/limitations">Coupling Limitations</a> for what these mean in practice.</p><h2 id="See-Also" tabindex="-1">See Also <a class="header-anchor" href="#See-Also" aria-label="Permalink to &quot;See Also {#See-Also}&quot;">​</a></h2><ul><li><p><a href="./internals">Coupling Internals</a> — how the monolithic right-hand side is assembled.</p></li><li><p><a href="./../guides/coupling/">Coupling Overview</a> — the user-facing model.</p></li></ul>',32)])])}const u=t(r,[["render",s]]);export{g as __pageData,u as default};
